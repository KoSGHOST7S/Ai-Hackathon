# Public hostname for HTTPS and reverse proxy.
DOMAIN=api.example.com

# Server/API
PORT=3000
JWT_SECRET=replace-with-long-random-secret
ENCRYPTION_SECRET=replace-with-32-ascii-char-secret

# Database (used by local prisma tasks; docker-compose overrides API to use db hostname)
DATABASE_URL=postgresql://assignmint:password@localhost:5432/assignmint?schema=public

# IBM watsonx (required by agents service)
WATSONX_API_KEY=replace-me
WATSONX_PROJECT_ID=replace-me
WATSONX_URL=https://us-south.ml.cloud.ibm.com/
GRANITE_MODEL=openai/gpt-oss-120b
